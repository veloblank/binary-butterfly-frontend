import { checkToggledButton } from "../components/Navbar.js";

window.load = addEventListeners()

function addEventListeners() {
  let radioButtons = document.getElementsByClassName('toggle-radio')
  console.log(radioButtons)
}